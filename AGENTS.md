# Agent Notes

- This is currently a notebook-only workspace: the sole working file is `Untitled.ipynb`; there is no application/package entrypoint.
- `Untitled.ipynb` is a valid nbformat 4.5 notebook with one **unexecuted** code cell (`execution_count: null`) containing `import matplotlib as plt`. Preserve the notebook structure and cell metadata unless a change requires otherwise.
- `.ipynb_checkpoints/Untitled-checkpoint.ipynb` is an empty Jupyter checkpoint, not a second source file or test suite.
- The notebook metadata records Python 3.12.10, but this is notebook metadata rather than a repository-wide Python pin.
- No README, dependency/environment manifest, tests, formatter, linter, type checker, build, CI, or pre-commit configuration is present. Do not invent repo-defined commands; verify the local environment before adding or relying on dependencies.
